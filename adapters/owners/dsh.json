{
  "id": "dsh",
  "_comment": "dsh 的 live 配置几乎都能热更：cordis.patch.yml（MCP 等）由 HMR watcher 写入即生效，AGENTS.md 由 agent-instructions 逐请求 reconcile。真正需要重启的只有插件集合（profiles/*/package.json）——由 restart-guard 的 classify 判定、空闲后自动重启。",
  "probe": { "any": ["~/.dsh/profiles", "~/.dsh/AGENTS.md"] },
  "reload": {
    "mode": "http",
    "guardBase": "http://127.0.0.1:3080",
    "classify": "/restart/classify?path={path}",
    "request": "/restart/request",
    "kind": "sync-converge",
    "hint": "patch/AGENTS 热重载；插件集合变更 → restart-guard 空闲后自动重启一次"
  }
}
