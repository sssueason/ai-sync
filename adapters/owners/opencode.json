{
  "id": "opencode",
  "_comment": "交互式 TUI：配置在启动时读取，进程不能被杀（会丢当前会话）⇒ 只通知、不自动重启。",
  "probe": { "any": ["~/.config/opencode", "~/.config/opencode/opencode.jsonc"] },
  "reload": { "mode": "manual", "hint": "重启 opencode 生效（配置在启动时读取）" }
}
