{
  "version": 3,
  "_comment": "模板：复制成 <实例根>/sync/instance.json 后按需改。**不要**把令牌/密码写在这里（令牌只进 auth 指定的凭据存储）。",

  "_tick": "节拍：tick 间隔（分钟，改完下次 tick 自动重排平台调度）、抖动上限（秒，多机错峰）、状态推送间隔（分钟，避免每轮都提交状态）",
  "tick": { "intervalMinutes": 5, "jitterSeconds": 45, "statePushMinutes": 15 },

  "_console": "本地控制台（向导/仪表盘/设置）。默认只绑 127.0.0.1；要局域网访问必须同时设 token 并自担风险",
  "console": { "enabled": true, "bind": "127.0.0.1", "port": 7788, "autostart": true, "token": "" },

  "_producers": "哪些渲染器参与（produces live 配置清单）。关掉某个 = 它的目标不再进入收敛",
  "producers": { "agentFiles": true, "mcp": true, "conventions": true },

  "_owners": "每个应用变了要不要重载/重启：见 adapters/owners/*.json 的 reload.mode（none|hot|http|command|manual）。2026-09-26：dsh 已转桌面端，其 owner 卡已撤除",
  "owners": { "opencode": true, "workbuddy": true, "claudian": false, "vault": true },

  "_cloudMirror": "云盘镜像（µ2）节拍：默认每天 22:00；也可写成 {\"mode\":\"interval\",\"intervalMinutes\":60}。enabled=false = 纯 git 同步",
  "cloudMirror": { "enabled": true, "schedule": { "mode": "dailyAt", "times": ["22:00"] } },

  "_triage": "免费模型只读分诊（可选，默认关）：条件命中时把诊断包的事实摘要交给模型，它只在固定枚举里选类别、在固定清单里选一条建议，**不执行任何动作**。注意：事实摘要（断言/状态/日志尾/git 状态，可能含路径与机器名）会发给你配置的模型服务；不接受就保持 enabled=false。模型不可用/超时/解析失败一律降级，不影响同步。",
  "triage": { "enabled": false, "model": "<你的模型 id>", "minIntervalMinutes": 60 },
  "_auth": "凭据来源：auto = Windows 凭据管理器 / macOS 钥匙串 / 回退 0600 文件；file = 指定文件；env = 环境变量",
  "auth": { "mode": "auto", "file": "~/.config/ai-sync/credentials" }
}
